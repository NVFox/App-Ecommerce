<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="https://unpkg.com/tailwindcss@2.2.19/dist/tailwind.min.css"/>
    <link href="https://fonts.googleapis.com/css?family=Work+Sans:200,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.2/font/bootstrap-icons.css">

    <link rel="stylesheet" href="/css/Perfil/perfil.css">
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <title><%= session.nombreUsuario %> - Perfil Ecultural</title>
</head>
<body>

    <%- include('./partials/header', {pages: pages, isIndex: false, session: session}) %>

<div>
    <form action="" class="info" id="formDatos">
        <div class="cont columna_2" >
            <div class="sec1">
                <div class="detalle">
                    <span class="s"><h1>Información Personal</h1></span>
                    <input type="text" class="field form" name="nombreUsuario" value="<%= session.nombreUsuario %>" id="form-nombreUsuario" placeholder="Nombre de Usuario">
                    <input type="password" class="field form" name="clave" id="form-clave" placeholder="Contraseña">
                    <input type="email" class="field form" name="correo" value="<%= session.correo %>" id="form-correo" placeholder="Correo">
                    <input type="text" class="field form" name="domicilio" value="<%= session.domicilio %>" id="form-domicilio" placeholder="Domicilio">
                    <input type="text" class="field form" name="numeroTelefono" value="<%= session.numeroTelefono %>" id="form-numeroTelefono" placeholder="Teléfono">
                    <select name="sexo" class="field form" name="sexo" id="form-sexo" required>
                        <option value="<%= session.sexo %>" selected><%= session.sexo === "M" ? 'Masculino' : 'Femenino' %></option>
                        <% ["M", "F"].filter(item => item !== session.sexo).map(opt => { %>
                            <option value="<%= opt %>"><%= opt === "M" ? 'Masculino' : 'Femenino' %></option>
                        <% }) %>
                    </select>
    
                    <button type="button" class="buton" id="btn-actualizar">Actualizar</button>
                    <button type="button" class="buton" id="btn-eliminar">Eliminar</button>
                </div>
            </div>
        </div>
        <div class="cont columna_1">
            <div class="img">
                <img src="/img/<%= session.imagen %>" alt="img-perfil">
            </div>
            <input type="file" class="field" name="imagen" id="form-imagen">
        </div>
    </form>
</div>

<script src="/js/CRUD/crudUtilities.js"></script>


</body>
</html>